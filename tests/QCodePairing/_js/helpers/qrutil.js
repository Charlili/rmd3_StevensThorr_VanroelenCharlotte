'use strict';

import 'array.from';

export const html5_qrcode = ($reader, readerWidth = 300, readerHeight = 250) => {

  let $reader = $reader;
  let $vidElem = $reader.querySelector('.qrvid');
  let $canvasElem = $reader.querySelector('.qrvid');

  console.log(readerWidth, readerHeight);

};

export const html5_qrcode_stop = () => {



};
